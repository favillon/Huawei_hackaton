# Prompts usados con GLM 5.2 — SentinelPay Risk Engine (RETO 4)

## Prompt 1 — Análisis del enunciado y arquitectura

```
Revisar el archivo Retos/RETO_4_SentinelPay_Risk_Engine.md. Haz un análisis del
objetivo e indica la tecnología y método más apropiados para implementar toda esta
lógica. Considerar código limpio y mejores prácticas. La carpeta "codigo" es donde
se implementa el código, "prompt_usado.txt" registra los prompts, "requerimientos.txt"
define las dependencias.
```

**Decisión de GLM 5.2:** Python 3.11 + FastAPI + Pydantic + httpx + Jinja2/HTMX.
Razón: async nativo para ráfagas, Pydantic para modelos tipados, un solo servidor
sirve API y GUI. GLM 5.2 es copiloto de desarrollo (no entra en runtime).

## Prompt 2 — Diseño de modelos de dominio

```
Definir los modelos Pydantic para SentinelPay: Transaction (id, card_id,
merchant_id, amount, currency, country, ip_country, device_id, ip, timestamp),
Reason (rule, weight, detail), EvaluationResult (transaction_id, score, decision,
reasons, bank_auth_status), RuleConfig (enabled, weight), ScoringConfig con pesos
por defecto del reto: amount=25, country=30, hour=10, velocity_card=40,
velocity_device=50. Umbrales: APPROVE <40, REVIEW 40-74, DECLINE >=75.
```

**Resultado:** Modelos creados en app/domain/models.py. Config con defaults en
app/config.py. Score cap a 100 con min(sum(weights), 100).

## Prompt 3 — Scaffold del proyecto

```
Crear la estructura del proyecto FastAPI para SentinelPay: app/__init__.py,
app/main.py con endpoint /health, run.py con uvicorn, requirements.txt con
fastapi, uvicorn, pydantic, pydantic-settings, httpx, jinja2, python-multipart,
pytest, pytest-asyncio.
```

**Resultado:** Servidor arranca en :8000. Verificado con import de app.main.

## Prompt 4 — Reglas estáticas (Fase 1)

```
Implementar 3 reglas estáticas configurables en app/rules/static.py:
1. amount_anomaly: si amount > 3x promedio histórico de la tarjeta → +25
2. country_mismatch: si country ≠ ip_country → +30
3. unusual_hour: entre 1am-5am → +10
Cada regla respeta enabled/weight desde ScoringConfig. Historial in-memory
dict[card_id, list[amount]]. Devolver list[Reason].
```

**Resultado:** StaticRuleEngine con 3 reglas. Verificado con ejemplo del reto:
CO/RU 4:58am → country_mismatch(+30) + unusual_hour(+10) = 40.

## Prompt 5 — Sliding window, velocity y blocklist (Fase 2)

```
Implementar detección de card testing con ventana deslizante:
1. SlidingWindow: deque por card_id con timestamps, ventana 10s
2. DeviceCardWindow: cuenta card_ids distintos por device_id, ventana 30s
3. Blocklist: dict[key, expiry] con auto-expiración lazy, TTL 120s
4. VelocityRuleEngine: velocity_card (>5 txns/10s → +40 y bloquea tarjeta),
   velocity_device (>3 cards/30s → +50 y bloquea dispositivo)
Todo protegido con asyncio.Lock. check_blocklist() para short-circuit DECLINE.
```

**Resultado:** 3 tests pasados:
- 6 txns en 7s → 6ª activa velocity_card(+40) + bloquea tarjeta
- Tarjeta bloqueada → check_blocklist devuelve card_temporarily_blocked
- Tras 120s → tarjeta desbloqueada automáticamente
- 4 cards distintas mismo device → velocity_device(+50)

## Prompt 6 — Scoring ponderado + circuit breaker (Fase 3)

```
Implementar scoring y resiliencia:
1. scoring.py: compute_score = min(sum(weights), 100), umbrales
   APPROVE <40, REVIEW 40-74, DECLINE >=75. Si circuit degradado →
   APPROVE degrada a REVIEW (DECLINE no se override).
2. circuit_breaker.py: 3 fallos consecutivos → OPEN 15s → SEMI_OPEN →
   trial call → success cierra / fail re-abre. asyncio.Lock protege estado.
3. mock_bank_auth.py: 30% falla o timeout (split entre error y timeout).
```

**Resultado:** Tests pasados:
- Score cap: 155 → 100 (DECLINE)
- Circuit degradation: APPROVE → REVIEW cuando circuit open
- DECLINE no se override por circuit degradation
- 3 fallos consecutivos → circuit OPEN → degraded sin llamar servicio
- Tras 15s → semi-open → trial success → CLOSED

## Prompt 7 — Orquestador evaluate() (Step 6)

```
Implementar RiskEngine en app/engine/evaluate.py que orquesta:
1. check_blocklist → si bloqueada, DECLINE inmediato (score=100, sin re-evaluar)
2. static rules → reasons
3. velocity rules → reasons (puede bloquear card/device)
4. bank auth via circuit breaker → bank_auth_status
5. build_result con scoring + decisión
Método get_state() para inspección de blocklist y circuit breaker.
```

**Resultado:** Tests end-to-end pasados:
- Transacción normal (CO/RU 4:58am) → score=40 REVIEW
- Transacción limpia → score=0 APPROVE
- 6 txns misma tarjeta → 6ª activa velocity_card(+40) + bloquea
- Txn #7 → DECLINE inmediato con card_temporarily_blocked
- Blocklist poblada correctamente, circuit breaker operativo

## Prompt 8 — Rutas API (Step 7)

```
Implementar rutas FastAPI en app/api/routes.py:
- POST /api/evaluate (single transaction → EvaluationResult)
- POST /api/evaluate/batch (list of transactions)
- POST /api/simulate/burst (N txns misma tarjeta, interval configurable)
- GET /api/config (ScoringConfig actual)
- GET /api/state (blocklist + circuit breaker)
RiskEngine singleton a nivel módulo para persistir estado entre requests.
Incluir router en app/main.py.
```

**Resultado:** Todos los endpoints verificados via curl:
- /api/evaluate CO/RU 4:58am → score=40 REVIEW ✅
- /api/simulate/burst 7x → txns 1-3 APPROVE, 4-5 REVIEW (circuit degrade),
  6 velocity_card(+40), 7 DECLINE card_temporarily_blocked ✅
- /api/config y /api/state retornan JSON correcto ✅
- Swagger UI en /docs con todos los schemas ✅
